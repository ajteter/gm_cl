# SPA fallback for client-side routing
# Static files and special routes (optimized order for performance)
/_headers  /_headers  200
/_redirects  /_redirects  200
/games.json  /games.json  200
/favicon.ico  /favicon.ico  200
/robots.txt  /robots.txt  200
/sitemap.xml  /sitemap.xml  200

# Static assets with splat
/assets/*  /assets/:splat  200

# SPA fallback - exclude index.html to prevent infinite loop
/index.html  /index.html  200
/*  /index.html  200