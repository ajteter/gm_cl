# 2026-04-17 改动评审

## 一、updatelog.md 准确性核验

我逐条将 `updatelog.md` 的描述与实际 `git diff` 进行了交叉比对。

| updatelog 条目 | 实际 diff 验证 | 结论 |
|:---|:---|:---|
| `.gitignore` 加入 `skills/` | ✅ diff 确认新增 `skills/` 一行 | **准确** |
| `SEO_GEO_OPTIMIZATION_PLAN.md` 新增 | ✅ untracked 文件列表中存在 | **准确** |
| LeaderboardSystem 分数/标题顺序调换 | ✅ diff 确认 `yourScore` 移到 `title` 上方 | **准确** |
| LeaderboardSystem 新增分享邀请按钮 | ✅ diff 确认 `buildInviteMessage`、`copyTextToClipboard`、`handleCopyInvite`、按钮 JSX | **准确** |
| 分享按钮 i18n 补齐 | ✅ `en-US.json` 新增 4 个 `leaderboard.invite*` key；locale 文件全部在 unstaged 列表中 | **准确** |
| Floppy Bird 首屏图片预加载 | ✅ `index.html` diff 确认 5 个 `<link rel="preload">` | **准确** |
| splash/scoreboard/replay 动画时间缩短 | ✅ `main.js` diff：splash `2000→500`，scoreboard/replay `600→250` | **准确** |
| Layout.jsx 清空为纯包装组件 | ✅ diff 删除 ~85 行 SEO 逻辑，仅保留 `return children` | **准确** |
| seoUtils.js 统一域名、robots、canonical 等 | ✅ diff 确认 `baseUrl` 硬编码为 `https://flybird.site`，新增 `robots` 字段、`resolveAbsoluteUrl`、`removeMetaTag`、`generateCollectionSEO`、`generateWebPageStructuredData`、`generateBreadcrumbStructuredData`、`generateCollectionStructuredData`；移除 `SearchAction` | **准确** |
| useSEO.js 增强（`useCollectionSEO`、`enabled` 开关、各页 metadata 补充） | ✅ diff 确认新增 `useCollectionSEO`、`options.enabled` 逻辑、首页 canonical 收敛到 `/floppybird`、play 页 `noindex` | **准确** |
| GamePage.jsx 修复双 SEO hook 冲突 | ✅ diff 确认拆分为条件性 `useGameSEO` + `useCollectionSEO` | **准确** |
| GameCard.jsx 标题改为内链 | ✅ diff 确认引入 `Link`、`<h2>` 内嵌 `<Link to={/game?id=...}>` | **准确** |
| index.html 补齐 canonical、og:url、og:site_name、Twitter Card、author | ✅ diff 逐项确认 | **准确** |
| robots.txt 域名修复 + `Disallow: /api/` | ✅ diff 确认 `yourdomain.com → flybird.site` + `/api/` | **准确** |
| sitemap.xml 更新路由与优先级 | ✅ diff 确认 5 条路由，`/floppybird` priority=1.0，`/` 降为 0.6 | **准确** |
| 删除 HomePage/GamePage 无用广告常量 | ✅ diff 确认两处 `gamePageAdConfig` / `homeAdConfig` 删除 | **准确** |

> **结论：updatelog.md 所有条目与实际代码 diff 完全一致，无漏记、无虚报。日志质量合格。**

---

## 二、代码质量与潜在问题

### ⚠️ 需要关注的问题

#### 1. `useSEO` hooks 在条件分支中使用 — React Rules of Hooks 违规风险

[GamePage.jsx](file:///Users/huxiao/Public/GitHub/gm_cl/static-site/src/pages/GamePage.jsx) 中：

```jsx
useGameSEO(selectedGame, {
  enabled: Boolean(gameId),
  ...
})
useCollectionSEO(currentGames, currentPage, { enabled: !gameId })
```

**两个 hook 都被无条件调用**（通过 `enabled` 参数内部控制是否生效），这种模式是正确的，不违反 React hooks 规则。

但**需要确认 `useSEO` 内部在 `enabled === false` 时是否执行了 cleanup**（清除前一次设置的 meta tag）。如果用户先访问列表页、再点进详情页，`useCollectionSEO` 变为 `enabled: false` 后，它写入的 structured data 是否会被清除？

> [!WARNING]
> 建议检查 `useSEO` 内部 `useEffect` 的 cleanup 函数，确保禁用时能移除上一轮注入的 `<script type="application/ld+json">` 节点，否则同一页面可能残留两套 structured data。

#### 2. `copyTextToClipboard` 的 `execCommand('copy')` 降级路径

[LeaderboardSystem.jsx](file:///Users/huxiao/Public/GitHub/gm_cl/static-site/src/components/LeaderboardSystem.jsx) 中的 fallback：

```js
try {
  document.execCommand('copy')
} finally {
  document.body.removeChild(textarea)
}
```

`execCommand('copy')` 可能返回 `false`（失败），但这里没有检查返回值。在 `finally` 块中 textarea 会被移除，但调用方 `handleCopyInvite` 会认为复制成功（因为没有抛出异常），继而显示"Copied!"。

> [!TIP]
> 建议在 `execCommand` 返回 `false` 时主动 `throw`：
> ```js
> if (!document.execCommand('copy')) {
>   throw new Error('execCommand copy failed')
> }
> ```

#### 3. `leaderboard.inviteMessage` 中的硬编码域名

```json
"leaderboard.inviteMessage": "I scored {score} in Floppy Bird! 🐦\nOnly Top 10 make the global leaderboard. Can you beat me?\n👉 flybird.site"
```

域名 `flybird.site` 被硬编码在 i18n 翻译字符串中。如果将来域名变更，需要同时改动 44 个 locale 文件。考虑到这是用户直接复制的分享文案而非技术 URL，目前这样做是合理的，但需要意识到这个维护成本。

#### 4. sitemap.xml 中 `lastmod` 缺失

新版 sitemap 删除了所有 `<lastmod>` 标签。虽然 `lastmod` 不是必须字段，但 Google 建议保留准确的 lastmod 以辅助增量抓取。对于像 `/game` 这种游戏列表经常变化的页面，有 lastmod 会更好。

#### 5. 首页 canonical 设为 `/floppybird` 的策略考量

`useHomeSEO` 将首页的 canonical 收敛到 `https://flybird.site/floppybird`，而 `sitemap.xml` 中也给了 `/floppybird` priority=1.0、`/` priority=0.6。

这意味着搜索引擎会将 `/floppybird` 视为主入口，而 `/` 是次要的发现入口。**这在当前项目只有一款核心游戏的情况下是合理的**。但如果将来增加更多游戏并希望首页成为聚合入口，这个策略需要反转。

---

### ✅ 做得好的部分

| 项目 | 评价 |
|:---|:---|
| **Layout.jsx 清空** | 移除了与 `useSEO` hook 层重复的 SEO 逻辑，消除了两套系统互相覆盖的 bug。干净利落。 |
| **`resolveAbsoluteUrl` 工具函数** | 统一处理相对/绝对路径转换，避免 `og:image` 写入相对路径导致社交分享预览图片失效。 |
| **`removeMetaTag` + image 清理** | `setPageSEO` 在无 image 时主动移除 `og:image` / `twitter:image`，解决了 SPA 路由切换时 meta 标签残留的问题。 |
| **Game canonical 使用 `?id=` 查询参数** | 之前的 `/game/{namespace}` 路由实际不存在，修正为 `/game?id=...` 与真实路由匹配。 |
| **移除 `SearchAction`** | 项目没有搜索功能，之前的 SearchAction JSON-LD 是虚假声明，移除后符合 Google 结构化数据指南。 |
| **LCP 预加载** | `sky.png` 与 `bird.png` 使用 `fetchpriority="high"` 是正确的做法；`scoreboard.png` 和 `font_big_0.png` 不加 high priority 也合理，因为它们不在首屏。 |
| **动画时间缩短** | splash 2000→500ms、scoreboard/replay 600→250ms，合理地缩短了非交互等待而不会让用户感觉突兀。 |
| **分享按钮 UX** | 使用 `copyButtonText` state + `setTimeout` 2s 复位的模式是时下常见的 "Copy + feedback" 交互，实现简洁。 |

---

## 三、总结建议

| 优先级 | 建议 |
|:---|:---|
| 🔴 **P0** | 检查 `useSEO` hook 内部在 `enabled: false` 时是否清理上一轮写入的 structured data 节点，避免详情页和列表页的 JSON-LD 同时残留在 DOM 中。 |
| 🟡 **P1** | `copyTextToClipboard` 的 `execCommand` 降级路径应检查返回值，在失败时抛出异常以正确触发 "Copy Failed" 提示。 |
| 🟢 **P2** | 考虑在 sitemap.xml 中恢复 `<lastmod>`，可以在构建时自动注入日期。 |
| 🟢 **P2** | 新增 locale 的 `leaderboard.invite*` 共 4 个 key 在 unstaged 状态，确认翻译质量后记得提交。 |
